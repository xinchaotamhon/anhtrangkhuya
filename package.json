{
  "name": "anh-trang-khuya",
  "version": "0.1.0",
  "private": true,
  "description": "PWA nhật ký phản tư local-first, không backend.",
  "scripts": {
    "start": "node tools/serve.mjs",
    "build": "node tools/build_deploy.mjs",
    "test": "node --test tests/*.test.js",
    "check:static": "node tools/check_static_app.mjs",
    "check:deploy": "node tools/check_deploy_bundle.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
