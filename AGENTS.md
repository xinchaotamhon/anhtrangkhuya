# Project Instructions

1. Read `START_HERE.md` and follow its read order before editing.
2. Use `$prepare-project-work` when available.
3. Prefer codebase-memory graph tools for indexed code discovery and impact tracing.
4. Treat chat and history as leads, not current truth.
5. Never mutate raw inputs or expose secrets.
6. Put volatile facts in state files and reproducible results in evidence files, not `START_HERE.md`.
7. Establish baseline, gate, backup and rollback before risky changes.
8. Run relevant tests and update evidence/state before claiming completion.
9. Routine continuation follows this project's local read order; do not require the Vault merely to recover current project context.
10. Search adopted resources before rebuilding. Consult the pinned Vault only for a new capability, candidate proposal or standard upgrade.
11. Record a reusable-tool candidate locally; never copy it directly into verified Vault content or grant it trust.
12. Logs aid diagnosis, but only assertions/tests/gates plus evidence prove behavior.
